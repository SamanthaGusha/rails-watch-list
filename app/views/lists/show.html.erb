<!--add a function that shows the comment written by the user
-->
<div class="container">
  <div class="text-center">
    <h2 class="section-heading text-uppercase"><%= @list.name %></h2>
    <%= link_to 'Add a movie to list', new_list_bookmark_path(@list) %>
  </div>
  <div class="row">
  <% @list.movies.each do |movie| %>
    <div class="card" style="width: 18rem;">
    <img src="https://images.unsplash.com/photo-1607604276583-eef5d076aa5f?w=800&auto=format&fit=crop&q=60&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxzZWFyY2h8M3x8YW5pbWV8ZW58MHx8MHx8fDA%3D" class= "card-img-top"alt="list-title">
      <div class="card-body">
        <% @bookmark = Bookmark.find_by(list: @list, movie: movie) %>
        <p><%= movie.title %></p>
        <p><%= movie.overview %></p>
        <p><%= movie.poster_url %></p>
        <p><%= movie.rating %></p>
        <%= link_to "Delete",
        bookmark_path(@bookmark),
        data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
      %>
      </div>
    </div>
  <% end %>
  </div>
  <%= link_to 'Back to main', lists_path(@list) %>
</div>
